{
  "buildCommand": "npm install --include=dev && cd frontend && npm install --include=dev && npm run build",
  "startCommand": "cd backend && node server.js",
  "devCommand": "npm run dev",
  "installCommand": "npm install --include=dev",
  "framework": "node",
  "nodeVersion": "20",
  "regions": ["fra1"],
  "environment": {
    "NODE_ENV": "production",
    "NPM_CONFIG_PRODUCTION": "false"
  }
}
